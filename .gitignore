# restore logs (may contain real schema/object names — never commit)
*.log
run.log

# local dumps and scratch
*.dump
/tmp/
/ele

# saved `pg_restore -l` plan listings (contain real object names — never commit).
# Root-scoped so committed test fixtures under internal/**/testdata are unaffected.
/*.toc
/*.loc

# goreleaser build output
/dist/

# `just build` output
/bin/
